import { StyleSheet } from "react-native";

export const marketStyles = StyleSheet.create({
    container: {
        backgroundColor: "#fff",
        padding: 15,
        height: "100%",
      },
    screenTitle: {
    color: "#000",
    fontSize: 20,
    fontWeight: "bold",
    textAlign: "center",
    },
    titleContainer: {
        margin: 10,
    },
    titleText: {
        color: "#ffcb05",
        fontSize: 16,
        fontWeight: "bold",
    },
    smallText: {
        color: "#ffcb05",
        fontSize: 12,
        marginVertical: 1,
    },
});