import { StyleSheet } from "react-native"

export const withdrawStyles = StyleSheet.create({
  container: {},
  logoContainer: {
    alignItems: "center",
  },
  logo: {
    width: 200,
    height: 200,
    marginTop: 65
  }
})
