import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    comparisonText: {
        fontSize: 20,
        color: '#ffcb05'
    },
    contentContainer: {
        display: 'flex',
        flexDirection: 'column',
        marginVertical: 25,
    },
});